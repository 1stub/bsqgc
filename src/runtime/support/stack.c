#include "stack.h"

thread_local Stack marking_stack = {NULL, NULL, NULL};

void stack_push_slow(Stack* s, void* obj)
{
    if(s->current == NULL) {
        xxxx;
    }
    else {
        xxxx;
    }
}